# Shared

This directory holds the shared profile and the files from group chats. Every
person can read them. Only Joshua writes here.

- `profile.md`: the shared profile. Nightly reflection rewrites it.
- `attachments/<group>/`: files that arrived in a group chat.

Reference notes go in the wiki, at `/data/wiki/`. Personal files live under
`people/<id>/`.
